struct Solution {}

#[allow(dead_code)]
impl Solution {
    pub fn find_kth_bit(n: i32, k: i32) -> char {
        todo!();
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        assert_eq!(Solution::find_kth_bit(3, 1), '0');
    }
}
